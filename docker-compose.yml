services:
  etcd:
    image: bitnamilegacy/etcd:${ETCD_IMAGE_TAG:-3.5.11}
    restart: unless-stopped
    environment:
      ALLOW_NONE_AUTHENTICATION: "yes"
      ETCD_ADVERTISE_CLIENT_URLS: "http://etcd:${ETCD_CLIENT_PORT}"
      ETCD_LISTEN_CLIENT_URLS: "http://0.0.0.0:${ETCD_CLIENT_PORT}"
    volumes:
      - etcd_data:/bitnami/etcd
    networks:
      - apisix
    healthcheck:
      test: [ "CMD-SHELL", "etcdctl --endpoints=http://127.0.0.1:${ETCD_CLIENT_PORT} endpoint health" ]
      interval: 10s
      timeout: 5s
      retries: 5

  apisix:
    image: apache/apisix:${APISIX_IMAGE_TAG:-3.16.0-debian}
    restart: unless-stopped
    depends_on:
      etcd:
        condition: service_healthy
    # APISIX, config.yaml içindeki ${{...}} değerlerini bu ortam
    # değişkenlerinden runtime'da okur (admin key + portlar).
    environment:
      APISIX_ADMIN_KEY: ${APISIX_ADMIN_KEY:?APISIX_ADMIN_KEY .env icinde tanimli olmali}
      APISIX_PROXY_PORT: ${APISIX_PROXY_PORT}
      APISIX_ADMIN_PORT: ${APISIX_ADMIN_PORT}
      ETCD_CLIENT_PORT: ${ETCD_CLIENT_PORT}
    volumes:
      - ./apisix_conf/config.yaml:/usr/local/apisix/conf/config.yaml:ro
    networks:
      - apisix

networks:
  apisix:
    driver: bridge

volumes:
  etcd_data:

